Problem:
1. 除彈跳電路的影響，對單按按鍵控制上有較好的改善效果，可以讓數值在人手按下去後彈起的時間內保持一次的數值，避免多次進入 function 做動
   |______可以用 counter 去處理

2. if else 不要寫太多，寫太多就要分成多個 always 去做，不然會有數值 latch 問題
3. 計數器根據 Board 的 clock 決定，eg. 50Mhz*20ns = 1s，其餘照此計算，因此 20ns 代表要計數 49999999 次後算一秒
